{
  "version": "independent",
  "npmClient": "yarn",
  "packages": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "ignoreChanges": [
        "*.md",
        "Makefile",
        "**/*.spec.*",
        "**/*.stories.*",
        "**/.jest",
        "**/jest.*.*",
        "**/jest.config.js",
        "**/.eslintrc.js",
        "./tools/**/*"
      ],
      "message": "chore(release): publish"
    }
  }
}
