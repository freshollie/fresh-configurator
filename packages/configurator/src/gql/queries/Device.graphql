query ConnectionState($port: String!) {
  device(port: $port) @client {
    connected
    connecting
  }
}

query Attitude($port: String!) {
  device(port: $port) @client {
    attitude {
      roll
      pitch
      heading
    }
  }
}
