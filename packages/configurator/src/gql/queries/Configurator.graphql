query SelectedPort {
  configurator @client {
    port
  }
}

query SelectedTab {
  configurator @client {
    tab
  }
}

query ExpertMode {
  configurator @client {
    expertMode
  }
}

query NavigationData {
  configurator @client {
    tab
    port
  }
}
