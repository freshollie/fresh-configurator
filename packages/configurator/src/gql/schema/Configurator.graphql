type Query {
  configurator: Configurator!
}

type Mutation {
  selectPort(port: String!): Boolean
  selectTab(tabId: String!): Boolean
  setExpertMode(enabled: Boolean!): Boolean
}

type Configurator {
  port: String
  tab: String
  expertMode: Boolean!
}
