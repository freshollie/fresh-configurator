// ts-config main, used to compile the electron main process without
// bundling all the typescript 
{
  "extends": "./tsconfig",
  "compilerOptions": {
    "outDir": "./app",
    "paths": {}
  },
  "include": ["src/main.ts"],
  "exclude": ["node_modules", "dist"]
}
