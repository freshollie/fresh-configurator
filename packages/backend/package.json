{
  "name": "@fresh/backend",
  "version": "1.0.0",
  "description": "The backend GraphQL server to retreive data from flight controllers",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "author": "freshollie",
  "license": "MIT",
  "scripts": {
    "start": "ts-node -T start.ts",
    "build": "rimraf dist && tsc -p tsconfig.build.json && cpx 'src/**/*.graphql' dist",
    "prepare": "yarn build"
  },
  "dependencies": {
    "@fresh/msp": "^1.0.0",
    "apollo-server": "^2.12.0",
    "graphql-tools": "^4.0.7",
    "merge-graphql-schemas": "^1.7.7",
    "uuid": "^7.0.3"
  },
  "devDependencies": {
    "@types/uuid": "^7.0.2",
    "cpx": "^1.5.0",
    "graphql": "^14.6.0",
    "rimraf": "^3.0.2"
  },
  "peerDependencies": {
    "typescript": ">=3.7",
    "graphql": "^14.0.0"
  }
}
